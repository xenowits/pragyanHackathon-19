const mocha = require('mocha')

const assert = require('assert')

const Farmer = require('../models/farmer')


describe('Saving records of Farmers', function(){

	it ('saves a farmer record to the database',function(done){


		assert('aman' + 'chutiya' === 'amanchutiya')
		done()

		// var char = new MarioChar({
		// 	name: 'Kutta',
		// 	weight: 15
		// })

		// char.save().then(function(){
		// 	assert(char.isNew === false);
		// 	done();
		// })

			// Farmer.findOne({district:'patna'}).then(function(result){

			// 	// console.log(result)

			// 	assert(result.district === 'patna')

			// 	done()
			// })

	})

	// next tests

})